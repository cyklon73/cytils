package io.github.cyklon73.cytils.api.discord.comp;

public class WelcomeScreenChannel {
    private String channel_id; //the channel's id
    private String description; //the description shown for the channel
    private String emoji_id; //the emoji id, if the emoji is custom
    private String emoji_name; //the emoji name if custom, the unicode character if standard, or null if no emoji is set
}
